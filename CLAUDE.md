# CLAUDE.md — інструкція для Claude по цьому проєкту

Цей файл — точка входу для будь-якого чату/сесії Claude, яка працює над цим проєктом.
Прочитай його першим, перед тим як щось редагувати.

## Що це за проєкт

**XPRNT STIKER IMPOSER** — локальний веб-застосунок для розкладки (імпозиції) прямокутних
наліпок на аркуш друку. Завантажується PDF-артворк → застосунок рахує сітку наліпок на
аркуші (SRA3/SRA3+ або кастомний розмір) → генерує:

- друк-готовий PDF (тайлінг артворку по сітці + мітки приводки),
- шаблон PDF з мітками (для повторного використання),
- файл різу `.plt` (HPGL) для плотера,
- опційно — векторний PDF контуру різу (для сторонніх програм розкрою).

Є batch-режим: кілька артворків за раз, з дедуплікацією однакових шаблонів сітки.
Все пакується в один zip.

Проєкт належить PaperFox і використовується у виробничому процесі (друк + плотерний різ).

## Стек

- **Backend:** Python 3.10+, FastAPI + Uvicorn
- **PDF:** `pypdf` (читання/компонування сторінок), `reportlab` (генерація шаблону й
  контуру різу), `PyMuPDF`/`fitz` (растрові мініатюри для прев'ю)
- **Frontend:** чистий HTML/CSS/vanilla JS, без фреймворків, `web/`
- **Запуск:** локально, `run_server.py` піднімає uvicorn і сам відкриває
  `http://127.0.0.1:8000` у браузері; `run.bat` — те саме одним кліком на Windows

## Структура коду

```
run_server.py, run.bat        — локальний лаунчер (сервер + автовідкриття браузера)
server/app.py                 — FastAPI app, монтування web/ як статики, підключення роутерів
server/models.py              — Pydantic-моделі запитів/відповідей
server/session_store.py       — тимчасове сховище завантажених PDF (in-memory + temp dir, TTL 2 год)
server/core/layout.py         — математика розкладки: auto-fit сітки, ручний override cols/rows/orientation
server/core/pdf_inspect.py    — визначення розміру артворку з PDF (TrimBox/MediaBox)
server/core/marks.py          — спільні мітки приводки (registration marks) для шаблону й контуру
server/core/pdf_template.py   — генерація шаблону PDF (мітки + підпис + невидима сітка комірок)
server/core/cut_contour_pdf.py— векторний PDF контуру різу (магента-лінії для сторонніх програм)
server/core/plt_writer.py     — генерація HPGL/.plt (дві стратегії: суцільні лінії сітки при gap=0,
                                 окремі прямокутники-комірки при gap>0)
server/core/print_pdf.py      — фінальний друк-PDF: тайлінг артворку по сітці, авто-поворот
                                 під орієнтацію комірки, contain-fit або деформація (deform)
server/core/packaging.py      — упаковка результатів у zip (одиночний і batch)
server/core/thumbnail.py      — растрове прев'ю першої сторінки PDF для UI
server/routes/                — API: /api/config, /api/analyze, /api/layout,
                                 /api/generate, /api/generate-batch, /api/session/{id}
server/utils/constants.py     — дефолтні відступи, пресети аркушів (SRA3/SRA3+), список матеріалів
server/utils/naming.py        — формування імен файлів/папок результату
web/                           — фронтенд: index.html (форма: артворк → аркуш/розмітка → замовлення),
                                 app.js (стан + виклики API + SVG-прев'ю), style.css
```

## Як запустити локально

```bash
pip install -r requirements.txt
python run_server.py
```

або подвійний клік на `run.bat` (Windows). Відкриється `http://127.0.0.1:8000`.

---

## ⚠️ Роль Claude в цьому чаті (Cowork) — контролер, не виконавець

Ця сесія Claude працює у застосунку (Cowork) з файловим мостом до цього комп'ютера —
вона бачить і читає файли проєкту, але **не має термінала** і не може сама виконувати
git/pip/запуск сервера. Тому з 2026-09-16 роль цього чату змінена:

- **Ця сесія НЕ редагує код проєкту напряму** (файли в `server/`, `web/`,
  `requirements.txt` тощо). 
- Реальне виконання задач — код, git branch/commit/push, встановлення залежностей,
  запуск і smoke test — робить **AI-агент у VS Code** на цьому ж комп'ютері (у нього
  є повний доступ до терміналу).
- Робочий цикл:
  1. Обговорюємо задачу в цьому чаті.
  2. Ця сесія читає обговорення й поточний стан файлів проєкту, формує **один
     закінчений промпт англійською** для агента у VS Code — за стандартом з
     [`PROMPTING.md`](./PROMPTING.md) (там повна структура промпту, кроки, приклади
     і розділ "Адаптація під XPRNT Sticker Imposer" з деталями саме цього проєкту).
  3. Користувач копіює промпт у VS Code, агент виконує все: код, git, PR, smoke test.
  4. Ця сесія за потреби перечитує змінені файли (тільки читання)

Якщо задача сформульована нечітко для одного точного промпту (не вкладається в ~10
кроків, або незрозуміло "де/що/як") — став уточнювальні питання в чаті, а не пиши
розмитий промпт.

### Перед тим як писати промпт

1. Прочитай `PROMPTING.md` повністю, якщо ще не читав у цій сесії.
2. Якщо потрібно перевірити актуальний стан коду — прочитай відповідні файли через
   файловий міст (без редагування). Не покладайся на пам'ять із попередніх чатів.
3. Сформуй промпт за структурою з `PROMPTING.md`, підставивши специфіку проєкту з його
   розділу "Адаптація під XPRNT Sticker Imposer" (стек, шляхи, base branch `main`,
   відсутність БД/тестів/Docker тощо).
4. Онови "Останній стан" нижче, якщо із задачі видно, що стан проєкту зміниться —
   або онови його вже після того, як агент відзвітує про виконання (Summary-блок з
   його промпту).

---

## Git-синхронізація

- **Репозиторій:** https://github.com/VolodymyrVlasov/XPRNT-Sticker-Imposer.git
- **Гілка за замовчуванням:** `main`
- Локально git ініціалізовано, перший коміт запушено (2026-09-16).

Відтепер усі git-операції (branch, commit, push, PR) виконує **агент у VS Code** як
частину промпту (STEP 0 / STEP N — COMMIT / STEP N+1 — PULL REQUEST з `PROMPTING.md`).
Ця Cowork-сесія git не виконує — вона лише формує промпт і за потреби читає стан
файлів.


Якщо колись працюєш без агента, вручну:
- Перед роботою: `git pull`
- Після роботи: `git add -A && git commit -m "..." && git push`

---

## Останній стан (оновлюється кожним суттєвим чатом)

- **2026-09-16** — Первинний аналіз проєкту.
  Функціонал станом на зараз: одиночний і batch режими розкладки наліпок, генерація
  PDF/PLT/contour, локальний запуск через `run_server.py`/`run.bat`. Git-репозиторій
  ініціалізовано і запушено на GitHub (гілка `main`, коміт "Initial commit").
  `.claude/settings.local.json` додано в `.gitignore` (видалення файлу з самого
  репозиторію — окрема дія, `git rm --cached`, ще не виконана). Робочий процес змінено:
  ця Cowork-сесія більше не редагує код напряму — вона формує промпти для AI-агента у
  VS Code за правилами з `PROMPTING.md` (новий файл, доданий цим чатом). Код проєкту
  цим чатом не змінювався.

- **2026-09-16** — `run.bat` зроблено стійким до відсутнього Python. Проблема: на
  користувача чекала Windows-заглушка App Execution Alias ("Python was not found;
  run without arguments to install from the Microsoft Store..."), яка на комп'ютері
  реально нічого не встановлювала. Тепер `run.bat` спочатку шукає `py -3`, потім
  `python`; якщо жодного не знайдено — друкує зрозумілу підказку (посилання на
  python.org + як вимкнути App execution alias), не намагаючись мовчки продовжити.
  Якщо Python знайдено — ставить/оновлює залежності глобально в системний Python
  (`pip install -r requirements.txt`) і лише потім запускає `run_server.py`.
  Виконано агентом у VS Code за промптом з цього чату: `run.bat` переписано,
  протестовано на реальному "порожньому" ПК (без Python — підтверджено чистий fail
  замість зависання) і на повному happy-path після встановлення Python 3.12.10
  (`winget`) — сервер піднявся, `/`, `/style.css`, `/app.js`, `/api/config` віддали
  200 OK. Закомічено й запушено напряму в `main` (мікро-задача, без feature-гілки й
  PR). Код цим чатом (Cowork) не редагувався, окрім цього файлу.

- **2026-09-16** — Старт розділу "Фігурні стікери" (розкладка фігурних наклейок з
  контуром порізки, який береться з PDF, а не генерується). План: 4 окремі промпти
  (backend-аналіз → frontend tab+прев'ю → backend генерація друк/PLT/contour-файлів),
  без папки-шаблону в результаті (лише друк-PDF + PLT, і опційно contour-PDF без
  растру). Бліди фіксовані 1мм з кожного боку, наклейки на аркуші — впритул (без gap).
  Задіює вже наявний, задизейблений таб сайдбару "Контур порізки" → перейменовується
  на "Фігурні стікери".

  **Етап 1 (backend-аналіз) — виконано й влито в `main`.** Гілка
  `feat/shape-analyze-backend`, PR влито користувачем вручну. Додано:
  `server/utils/constants.py` (`SHAPE_BLEED_MM = 1.0`),
  `server/core/shape_inspect.py` (`inspect_shape_pdf` — валідує наявність растру й
  вектору, витягує контур різу як підшляхи з ліній/безьє-кривих, координати
  нормалізовані відносно bleed-inclusive тайлу), `server/models.py`
  (`ContourSegment`/`ContourSubpath`/`ContourGeometry`/`ShapeAnalyzeResponse`),
  `server/routes/analyze_shape.py` (`POST /api/analyze-shape`, завжди `gap=0`).
  Існуючий прямокутний флоу не чіпався. Smoke test на синтетичних PDF підтвердив:
  бліди рахуються правильно (контур 40×30мм → `dim_w/dim_h` 42×32мм), координати
  контуру проінсетовані рівно на 1мм від країв тайлу, вісь Y у PyMuPDF
  (`get_drawings`/`get_images`) співпадає з конвенцією проєкту (top-left, Y вниз) —
  фліп не потрібен. Валідація обох напрямків (тільки растр / тільки вектор) віддає
  400 з очікуваними повідомленнями.

  Наступні етапи (ще не почато): 2 — фронтенд (активація табу, прев'ю контуру
  пунктиром в SVG, прибрати ручне редагування розміру); 3 — генерація друк-PDF (без
  вектору) + PLT + опційний contour-PDF, без папки-шаблону; 4 — PLT-трасування
  довільної форми з апроксимацією кривих.
